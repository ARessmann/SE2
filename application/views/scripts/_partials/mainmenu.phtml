<?php

$mnupoints = array ('index' => array ('/index', 'Veranstaltung'), 
					'statistics' => array('/statistics', 'Statistik')
				    );

echo '<ul class="nav">';

foreach ($mnupoints as $key => $point) {
	echo '<li ' . (($key == $this->controller) ? 'class="active"' : '') . '><a href="' . $point[0] . '">' . $point[1] . '</a></li>';
}

echo '</ul>';
    	
